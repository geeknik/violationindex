{
  "name": "@violation-index/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:check": "tsc --noEmit",
    "dev": "npx web-ext run --source-dir build",
    "test": "node --test"
  },
  "dependencies": {
    "@violation-index/shared": "*"
  },
  "devDependencies": {
    "esbuild": "0.25.2"
  }
}
